# VSCode Stimulator environment example
# Copy to .env locally if needed. Do not commit real secrets.

SECRET_KEY=change-this-to-a-long-random-secret
# Set this to enable the enterprise login gate. Leave blank/unset for open local use.
IDE_PASSWORD=change-this-password
WORKSPACE_ROOT=workspace
PORT=5000
